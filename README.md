# CUDA example

## Task

integrate f(x) on [a;b] using NVIDIA CUDA

## Requirements

* NVIDIA GPU with CUDA support
* CUDA runtime

## Build

* change dir to `build`
* run `cmake ..`
* run `make`

## Run

* run `cuda_example` from `build` dir

